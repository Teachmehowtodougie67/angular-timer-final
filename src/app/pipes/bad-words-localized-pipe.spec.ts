import { BadWordsLocalizedPipe } from './bad-words-localized-pipe';

describe('BadWordsLocalizedPipe', () => {
  it('create an instance', () => {
    const pipe = new BadWordsLocalizedPipe();
    expect(pipe).toBeTruthy();
  });
});
