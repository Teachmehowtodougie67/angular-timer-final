import { CursingwordsPipe } from './cursingwords-pipe';

describe('PipePipe', () => {
  it('create an instance', () => {
    const pipe = new CursingwordsPipe();
    expect(pipe).toBeTruthy();
  });
});
